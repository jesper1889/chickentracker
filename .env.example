# Database Configuration
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Example: postgresql://myuser:mypassword@localhost:5432/chickentracker
DATABASE_URL="postgresql://user:password@localhost:5432/chickentracker"

# NextAuth.js Configuration
# Application base URL
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# NextAuth secret for signing JWT tokens
# IMPORTANT: Generate a secure random secret using the command below
# Command: openssl rand -base64 32
# This should be a unique, random string - DO NOT use the placeholder value in production
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Instructions:
# 1. Copy this file to .env.local
# 2. Update DATABASE_URL with your PostgreSQL credentials
# 3. Generate a secure NEXTAUTH_SECRET using: openssl rand -base64 32
# 4. Update NEXTAUTH_URL for production deployment
