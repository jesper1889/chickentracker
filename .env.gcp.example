# Google Cloud Platform Configuration
# Copy this file to .env.gcp and fill in your actual values

# GCP Project Settings
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
GCP_SERVICE_NAME=chickentracker

# Cloud SQL Database Settings
GCP_DB_INSTANCE=chickentracker-db
GCP_DB_NAME=chickentracker
GCP_DB_USER=chickentracker
GCP_DB_PASSWORD=your-secure-password

# Database URL for Cloud SQL (will be set automatically)
# Format: postgresql://USER:PASSWORD@/DATABASE?host=/cloudsql/PROJECT:REGION:INSTANCE
DATABASE_URL=postgresql://${GCP_DB_USER}:${GCP_DB_PASSWORD}@/${GCP_DB_NAME}?host=/cloudsql/${GCP_PROJECT_ID}:${GCP_REGION}:${GCP_DB_INSTANCE}

# NextAuth Configuration
NEXTAUTH_SECRET=your-random-secret-key-min-32-characters
# This will be set after first deployment
NEXTAUTH_URL=https://your-cloud-run-url.run.app

# Optional: Service Account (if not using default)
# GCP_SERVICE_ACCOUNT=your-service-account@your-project.iam.gserviceaccount.com
